<div class="modal" id="eul_modal" tabindex="-1" role="dialog" aria-labelledby="createModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal-title">Gebruikersovereenkomst</h5>
                  {{ form_start(eul_form, {'action': path('eul_submit')}) }}
{#                <button type="submit" class="btn btn-primary" data-toggle="modal" data-target="#eul_modal">Akkoord</button>#}
{#                <button type="button" class="close" data-dismiss="modal">&times;</button>#}
                  {{ form_end(eul_form) }}
            </div>
            <div class="modal-body">
                {{ agreement_text|raw }}
                <div class="modal-footer">
                </div>

            </div>

        </div>
    </div>
</div>