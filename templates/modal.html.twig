<div class="modal show" id="eul_modal" tabindex="-1" role="dialog" aria-labelledby="createModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="modal-title">User Agreement</h5>
            </div>
            <div class="modal-body">
                <p>Lorem ipsum</p>
                <div class="modal-footer">
                </div>
                {{ form_start(eul_form, {'action': path('eul_submit'), 'method': 'PUT'}) }}
                     <button type="submit" class="btn btn-primary" data-dismiss="modal" data-target="#eul_modal">Akkoord</button>
                {{ form_end(eul_form) }}

            </div>

        </div>
    </div>
</div>